campo.Likes.updateLike('<%= @likeable.class.name.downcase %>', <%= @likeable.id %>, false);
$('#like-for-<%= @likeable.class.name.downcase %>-<%= @likeable.id %> .count').text('<%= @likeable.likes.count > 0 ? @likeable.likes.count : nil %>');
$('#like-for-<%= @likeable.class.name.downcase %>-<%= @likeable.id %>').closest('.list-group-item-actions').find('\
   .user-list #like-user-<%= current_user.id %>-avatar').remove();
